PyMuPDF>=1.24       # PDF page rendering (no poppler needed; Windows-friendly)
qdrant-client>=1.12 # multivector collection + upsert
requests>=2.31      # call the Modal embedder
vercel_blob>=0.4    # upload page images to Vercel Blob
python-dotenv>=1.0  # load .env
